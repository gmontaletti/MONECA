# Union-Find Data Structure for Efficient Group Merging

Implements a disjoint-set union (DSU) with path compression and union by
rank for O(alpha(n)) amortized operations where alpha is the inverse
Ackermann function.

## Usage

``` r
create_union_find(n)
```
