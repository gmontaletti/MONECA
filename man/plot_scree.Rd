% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density_reduction.R
\name{plot_scree}
\alias{plot_scree}
\title{Scree Plot for Component Selection}
\usage{
plot_scree(
  mx,
  max_components = 50,
  variance_target = 0.75,
  show_elbow = TRUE,
  title = NULL
)
}
\arguments{
\item{mx}{A mobility matrix with totals row/column}

\item{max_components}{Maximum number of components to display. Default is 50.}

\item{variance_target}{Optional numeric (0-1) to show target variance line.
Default is 0.75.}

\item{show_elbow}{Logical indicating whether to highlight the elbow point.
Default is TRUE.}

\item{title}{Optional plot title}
}
\value{
A ggplot2 object
}
\description{
Creates a ggplot2 visualization of variance explained per component,
useful for selecting the number of components (k) for dimensionality reduction.
}
\examples{
# Generate test data
data <- generate_mobility_data(n_classes = 30, seed = 123)

# Basic scree plot
plot_scree(data)

# With custom variance target
plot_scree(data, variance_target = 0.80)

# Without elbow detection
plot_scree(data, show_elbow = FALSE)

}
\seealso{
\code{\link{reduce_density}} for using the selected k
}
