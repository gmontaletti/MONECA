% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cutoff_rr_visualization.R
\name{plot_cutoff_analysis}
\alias{plot_cutoff_analysis}
\title{Plot Cut-off Sensitivity Analysis}
\usage{
plot_cutoff_analysis(
  sensitivity_results,
  optimal_cutoff = NULL,
  metrics = c("strength_ratio", "total_strength", "n_edges", "strength_cv"),
  colors = c("#2E86AB", "#A23B72", "#F18F01", "#C73E1D")
)
}
\arguments{
\item{sensitivity_results}{Output from cutoff_sensitivity_analysis()}

\item{optimal_cutoff}{Optional optimal cut-off value to highlight}

\item{metrics}{Character vector of metrics to plot. Default includes key metrics}

\item{colors}{Color palette for the plots}
}
\value{
ggplot2 object with multiple panels
}
\description{
Creates a multi-panel plot showing how various metrics change with cut-off values.
}
\examples{
\donttest{
# Generate synthetic mobility data  
data <- generate_mobility_data(n_classes = 4, seed = 123)

# Run sensitivity analysis
sensitivity <- cutoff_sensitivity_analysis(data, n_points = 20, verbose = FALSE)

# Find optimal cut-off
optimal <- find_optimal_cutoff(data, criterion = "elbow", verbose = FALSE)

# Plot sensitivity analysis with optimal point
if (require(ggplot2, quietly = TRUE) && require(tidyr, quietly = TRUE)) {
  p <- plot_cutoff_analysis(sensitivity, optimal$optimal_cutoff)
  print(p)
}
}

}
